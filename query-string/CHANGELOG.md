# Changelog

## 0.1.0
- First release. URL query string builder & parser.
- Query → JSON (repeated keys → arrays) and JSON → Query with encoding.
- Handles bare flags, leading ?/#, + as space.
- Dark mode, per-mode memory, one-click copy.
- Headless test suite (8 checks, incl. round-trips) + CI.
