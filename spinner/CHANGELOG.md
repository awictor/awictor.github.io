# Changelog

## 0.1.0
- First release. CSS loading spinner generator.
- Size, thickness, spinner/track colors, and speed with a live animated preview.
- Copy-ready `.spinner` class + `@keyframes spin`.
- 100% client-side. Dark mode, theme persistence.
- Headless test suite (10 checks) + CI.
