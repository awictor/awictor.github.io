# OS / editor cruft — this is a single self-contained HTML file with no build step.
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# Local scratch (harness / syntax-check files, if ever created)
_check.js
_harness.js
