# Changelog

## 0.1.0
- First release. JSON array ⇄ JSONL converter.
- Array → JSONL (compact per line) and JSONL → pretty array, both directions.
- Blank-line skipping and precise bad-line reporting; record counts.
- 100% client-side. Dark mode, theme persistence.
- Headless test suite (10 checks) + CI.
