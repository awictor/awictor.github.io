# Changelog

## 0.1.0
- First release. Cryptographically-random UUID v4 generator (`crypto.getRandomValues`).
- Single + bulk generate (up to 1000), copy all.
- Validate any UUID and detect version & variant; nil UUID handled.
- Dark mode, click-to-copy.
- Headless test suite (5 checks incl uniqueness) + CI.
