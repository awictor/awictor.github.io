# Changelog

## 0.1.0
- First release. JSON to TypeScript interface generator.
- Nested objects become named interfaces; arrays infer element types.
- Primitive/array roots emit a type alias; invalid keys quoted.
- Custom root name, one-click copy, dark mode, localStorage memory.
- Headless test suite (10 checks) + CI.
