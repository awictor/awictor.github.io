# OS / editor cruft — this is a zero-build, single-file project, so there is
# nothing to compile or install; only keep local junk out of the repo.
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.vscode/
.idea/
