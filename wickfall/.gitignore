# OS / editor cruft — this is a zero-build single-file project, so there is
# nothing to compile, but keep local machine junk out of the repo.
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/
node_modules/
